// This is a basic service worker file.
// It's required for the PWA functionality, especially for development and preview.

self.addEventListener('fetch', (event) => {
  // Basic fetch handler. For a full offline experience, you'd need to
  // implement caching strategies here.
});
